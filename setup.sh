#!/bin/bash
# The following line is commented out because en_core_web_lg is not used by the application,
# and it can cause permission issues during deployment on Streamlit Cloud.
# python -m spacy download en_core_web_lg
